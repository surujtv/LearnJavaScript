/*
    Some Predefined Object --> 
    1. Math Object
    - Properties: E, PI, SQRT2, SQRT1_2
    - Methods : abs(), acos(), asin(), atan(), ceil(), cos(), exp(), floor(), log(), max(), min() , pow(), random(), round(), sin()
    Example: console.log(Math.PI); // prints the value of Pi
    
    2. Global object (window in browser)
    - properties: isNaN(), isFinite()
    Example: console.log(isNaN("Hello")); // returns true if not a number
    
    3. Date object
    - Methods: getDate(), getDay(), getHours(), getMinutes(), getSeconds()
    toString(), toLocaleString(), UTC()
    Example: var today = new Date();
    console.log(today.getHours()); //prints current hour
*/
